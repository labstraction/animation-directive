import { FatBtnDirective } from './fat-btn.directive';

describe('FatBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new FatBtnDirective();
    expect(directive).toBeTruthy();
  });
});
